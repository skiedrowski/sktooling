package com.github.skiedrowski.tools.kt.compiler.allopen

/** this annotation is used by the kotlin all-open compiler plugin to identify classed which should be non-final
 * although they do not carry a respective annotation. */
annotation class AllOpen