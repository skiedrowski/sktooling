package com.github.skiedrowski.tools.kt.compiler.noarg

/** this annotation is used by the kotlin no-arg compiler plugin to identify classed which should have a default
 * constructor although they do not carry a respective annotation. */
annotation class NoArg